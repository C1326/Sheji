# 线上日记项目设计文档
> 项目名称：线上日记系统
> 技术栈：前端 Vue3 + Element‑Plus / Ant Design Vue；后端 SpringBoot + MyBatis‑Plus；数据库 MySQL；文件存储 本地/对象存储OSS；接口文档 Knife4j(Swagger)
> 核心功能：用户写日记，支持文本、图片、视频；日记公开授权，其他用户可查看、下载公开日记资源

## 1 项目概述
### 1.1 项目背景
线上日记是一个云端日记记录平台，用户可以在线撰写个人日记，除文字外可上传图片、视频附件；用户可自主设置日记权限为私有 / 公开，公开日记允许其他用户浏览，并且可以下载日记中的图片、视频资源；私有日记仅本人可见。

### 1.2 目标用户
1. 普通记录用户：撰写、管理自己日记，管理日记权限，上传图文视频
2. 浏览用户：查看他人公开日记，下载公开日记的媒体资源

### 1.3 功能范围
✅ 用户注册登录、个人信息管理
✅ 日记CRUD：新增、编辑、删除、查看日记
✅ 日记内容：富文本文本 + 多张图片 + 视频上传
✅ 日记权限控制：私有、公开两种权限
✅ 公开日记广场：浏览全部公开日记
✅ 公开日记：查看详情、下载图片/视频附件
✅ 个人日记管理列表
✅ 文件上传、访问、下载
❌ 暂不做：评论、点赞、关注、消息通知（可作为迭代版本）

## 2 整体架构设计
### 2.1 技术选型
|模块|技术选型|说明|
|---|---|---|
|前端|Vue3 + Vite|页面框架|
|UI组件库|Element‑Plus|表单、弹窗、上传组件|
|网络请求|Axios|请求后端接口|
|路由|Vue‑Router4|页面跳转|
|状态管理|Pinia|存储登录用户信息|
|后端|SpringBoot 3.x|服务主体|
|ORM|MyBatis‑Plus|数据库操作|
|数据库|MySQL8.0|业务数据存储|
|文件存储|本地存储 / 阿里云OSS|图片、视频文件|
|接口文档|Knife4j|接口调试|
|权限认证|JWT Token|登录鉴权|

### 2.2 架构分层
1. **前端层(Vue)**：页面渲染、表单输入、文件选择上传、权限判断、下载功能
2. **控制层(Controller)**：接收前端请求，参数校验，返回结果
3. **业务层(Service)**：业务逻辑，权限校验、文件处理逻辑
4. **数据访问层(Mapper)**：操作MySQL
5. **存储层**：MySQL存业务元数据；磁盘/OSS存储图片视频二进制文件

### 2.3 业务流程总览
1. 用户注册登录获取JWT令牌
2. 用户创建日记，填写文本，上传图片、视频；设置日记权限（私有/公开）
3. 保存日记元数据，文件保存，数据库记录文件路径
4. 个人中心查看自己全部日记；私有日记仅本人访问
5. 公开日记广场加载所有权限为公开的日记
6. 其他用户打开公开日记详情，查看文本、图片、视频；可以下载媒体资源
7. 用户可以编辑修改日记内容、权限、删除日记

## 3 数据库设计
### 3.1 用户表 user
|字段|类型|说明|
|---|---|---|
|user_id|bigint|主键自增|
|username|varchar(50)|账号，唯一|
|password|varchar(100)|加密密码|
|nickname|varchar(50)|昵称|
|avatar|varchar(255)|头像文件路径|
|create_time|datetime|创建时间|
|update_time|datetime|更新时间|

### 3.2 日记表 diary
> 日记主体，保存文本内容、权限、归属用户
|字段|类型|说明|
|---|---|---|
|diary_id|bigint|主键|
|user_id|bigint|所属用户id，外键关联user|
|title|varchar(100)|日记标题|
|content|text|日记富文本内容|
|permission|tinyint|0私有；1公开|
|create_time|datetime|日记创建时间|
|update_time|datetime|日记修改时间|

### 3.3 日记媒体文件表 diary_media
> 一篇日记可以有多个图片/视频，单独存储文件信息
|字段|类型|说明|
|---|---|---|
|media_id|bigint|主键|
|diary_id|bigint|关联日记id|
|media_type|tinyint|1图片；2视频|
|file_name|varchar(255)|原始文件名|
|file_path|varchar(500)|服务器存储路径/OSS地址|
|file_size|bigint|文件大小字节|
|create_time|datetime|上传时间|

> 说明：
> 1. 一篇日记对应多条diary_media记录，一对多关系
> 2. 下载资源时，根据mediaId先校验日记是否公开或者是否属于本人，校验通过返回文件流

### 3.4 数据库建表 SQL 脚本
> MySQL 8.0，字符集 utf8mb4，存储引擎 InnoDB

```sql
-- 创建数据库
CREATE DATABASE IF NOT EXISTS `sheji_diary`
  DEFAULT CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;

USE `sheji_diary`;

-- 删除旧表（注意依赖顺序）
DROP TABLE IF EXISTS `diary_media`;
DROP TABLE IF EXISTS `diary`;
DROP TABLE IF EXISTS `user`;

-- 1. 用户表
CREATE TABLE `user` (
  `user_id`     BIGINT       NOT NULL AUTO_INCREMENT COMMENT '主键',
  `username`    VARCHAR(50)  NOT NULL COMMENT '账号，唯一',
  `password`    VARCHAR(100) NOT NULL COMMENT '加密密码',
  `nickname`    VARCHAR(50)  DEFAULT NULL COMMENT '昵称',
  `avatar`      VARCHAR(255) DEFAULT NULL COMMENT '头像文件路径',
  `create_time` DATETIME     DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` DATETIME     DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`user_id`),
  UNIQUE KEY `uk_username` (`username`)
) ENGINE = InnoDB DEFAULT CHARSET = utf8mb4 COLLATE = utf8mb4_unicode_ci COMMENT = '用户表';

-- 2. 日记表
CREATE TABLE `diary` (
  `diary_id`    BIGINT       NOT NULL AUTO_INCREMENT COMMENT '主键',
  `user_id`     BIGINT       NOT NULL COMMENT '所属用户id，外键关联user',
  `title`       VARCHAR(100) NOT NULL COMMENT '日记标题',
  `content`     TEXT         COMMENT '日记富文本内容',
  `permission`  TINYINT      NOT NULL DEFAULT 0 COMMENT '0私有；1公开',
  `create_time` DATETIME     DEFAULT CURRENT_TIMESTAMP COMMENT '日记创建时间',
  `update_time` DATETIME     DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '日记修改时间',
  PRIMARY KEY (`diary_id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_permission` (`permission`),
  CONSTRAINT `fk_diary_user` FOREIGN KEY (`user_id`) REFERENCES `user` (`user_id`)
) ENGINE = InnoDB DEFAULT CHARSET = utf8mb4 COLLATE = utf8mb4_unicode_ci COMMENT = '日记表';

-- 3. 日记媒体文件表
CREATE TABLE `diary_media` (
  `media_id`    BIGINT       NOT NULL AUTO_INCREMENT COMMENT '主键',
  `diary_id`    BIGINT       NOT NULL COMMENT '关联日记id',
  `media_type`  TINYINT      NOT NULL COMMENT '1图片；2视频',
  `file_name`   VARCHAR(255) NOT NULL COMMENT '原始文件名',
  `file_path`   VARCHAR(500) NOT NULL COMMENT '服务器存储路径/OSS地址',
  `file_size`   BIGINT       DEFAULT 0 COMMENT '文件大小字节',
  `create_time` DATETIME     DEFAULT CURRENT_TIMESTAMP COMMENT '上传时间',
  PRIMARY KEY (`media_id`),
  KEY `idx_diary_id` (`diary_id`),
  CONSTRAINT `fk_media_diary` FOREIGN KEY (`diary_id`) REFERENCES `diary` (`diary_id`) ON DELETE CASCADE
) ENGINE = InnoDB DEFAULT CHARSET = utf8mb4 COLLATE = utf8mb4_unicode_ci COMMENT = '日记媒体文件表';
```

> 说明：
> 1. `user` 为 MySQL 保留字，建表时需使用反引号包裹
> 2. `diary_media` 与 `diary` 建立外键级联删除：删除日记时自动清理其媒体记录
> 3. 常用查询示例：
>    ```sql
>    -- 公开广场：按时间倒序分页
>    SELECT * FROM diary WHERE permission = 1 ORDER BY create_time DESC LIMIT 0, 12;
>    -- 我的日记
>    SELECT * FROM diary WHERE user_id = 1 ORDER BY create_time DESC;
>    -- 日记详情附带媒体资源
>    SELECT * FROM diary_media WHERE diary_id = 1;
>    ```

## 4 接口设计
### 4.1 用户模块
|接口|请求方式|说明|
|---|---|---|
|`/api/user/register`|POST|用户注册|
|`/api/user/login`|POST|登录返回JWT token|
|`/api/user/info`|GET|获取当前登录用户信息|

### 4.2 日记模块
|接口|请求方式|说明|权限校验|
|---|---|---|---|
|`/api/diary/add`|POST|新增日记，携带标题、content、permission|登录用户|
|`/api/diary/update`|PUT|修改日记内容、权限|只能修改自己日记|
|`/api/diary/delete/{diaryId}`|DELETE|删除日记|只能删除自己日记|
|`/api/diary/my/list`|GET|获取我自己所有日记列表|登录用户|
|`/api/diary/public/list`|GET|获取公开日记广场列表|无需登录|
|`/api/diary/{diaryId}`|GET|日记详情|私有仅本人；公开所有人|

### 4.3 文件上传下载模块
|接口|请求方式|说明|权限校验|
|---|---|---|---|
|`/api/media/upload`|POST|上传图片/视频，返回media对象|登录用户|
|`/api/media/download/{mediaId}`|GET|下载图片视频|日记必须公开 or 属于本人|

> 重点：**下载接口必须做权限拦截！**
> 根据mediaId拿到diaryId，查询diary表：
> - 如果日记permission=1公开 → 允许下载
> - 如果日记permission=0私有，判断当前登录user_id是否等于日记的user_id，相等允许下载，否则403拒绝访问

## 5 前端页面设计（Vue）
### 页面清单
1. 登录注册页 `/login`
2. 首页（公开日记广场） `/home`
    - 展示所有公开日记卡片：标题、作者昵称、缩略图、创建时间；点击跳转日记详情
3. 写日记页面 `/diary/write`
    - 富文本编辑器输入文本
    - 上传多张图片、上传视频
    - 权限单选框：【私有】【公开】
    - 保存、提交按钮
4. 个人中心 `/personal`
    - 我的日记列表，编辑、删除按钮
5. 日记详情页 `/diary/detail/:diaryId`
    - 展示日记标题、文本、图片预览、视频播放
    - 对公开日记：每个媒体文件显示【下载】按钮；私有日记只有本人可见下载按钮

### 前端关键组件
1. 富文本编辑器：`vue3-quill` 或 `wangeditor`
2. 文件上传：Element‑Plus `el‑upload`，限制图片、视频格式
3. 视频播放：原生`<video>`标签播放视频
4. 下载逻辑：调用后端 `/api/media/download/{mediaId}`

### 前端权限逻辑
- 用户未登录：只能看公开广场、公开日记详情；不能写日记、不能下载
- 登录用户：可以写日记；自己私有日记仅自己看到；公开日记可以浏览下载

## 6 后端核心业务逻辑说明
### 6.1 文件上传逻辑
1. 前端选择图片/视频，向后端上传
2. 后端校验文件类型：图片 jpg/png/jpeg；视频 mp4
3. 保存文件到本地或者OSS，生成访问路径
4. 返回 media 对象给前端；前端保存mediaId，提交日记的时候，把mediaId列表一起提交，批量保存diary_media表

### 6.2 权限校验逻辑（重要）
所有获取日记详情、下载媒体的接口，都必须校验权限：
```java
//伪代码
Diary diary = diaryMapper.selectById(diaryId);
//日记私有
if(diary.getPermission() == 0){
    //判断当前登录用户是否日记拥有者
    if(!loginUserId.equals(diary.getUserId())){
        throw 403禁止访问;
    }
}
//公开则放行
